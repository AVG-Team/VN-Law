module.exports = {
    plugins: {
        tailwindcss: {
            // eslint-disable-next-line no-undef
            config: join(__dirname, "tailwind.config.js"),
        },
        autoprefixer: {},
    },
};
